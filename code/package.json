{
  "name": "srcs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc && cp -r ./srcs/static ./dist/",
    "start": "node dist/srcs/backend/main.js",
    "dev": "npm run build && npm run start | pino-pretty --colorize",
    "dev:chalk": "npm run build && npm run start",
    "dev:watch": "ts-node-dev --respawn --transpile-only backend/main.ts | pino-pretty --colorize",
    "clean": "rm -rf ./dist",
    "redev": "npm run clean; npm run build && npm run start | pino-pretty --colorize",
    "redev:watch": "npm run clean; ts-node-dev --respawn --transpile-only backend/main.ts | pino-pretty --colorize"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/static": "^8.2.0",
    "@fastify/view": "^11.1.1",
    "animejs": "^4.1.3",
    "chalk": "^4.1.2",
    "ejs": "^3.1.10",
    "fastify": "^5.6.0",
    "qrcode-terminal": "^0.12.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/ejs": "^3.1.5",
    "@types/node": "^24.2.1",
    "@types/qrcode-terminal": "^0.12.2",
    "pino-pretty": "^13.1.1",
    "ts-node-dev": "^2.0.0"
  }
}
